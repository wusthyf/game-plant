# 植物精灵开发状态

更新日期：2026-09-05

项目的里程碑、任务编号、风险和 Git 工作流统一维护在仓库根目录的 `PROJECT_PLAN.md`。本文件记录游戏工程的技术交接状态。

## 当前基线

当前 Unity 工程是“GGJ48H 可玩 Demo”基线，不是 V1.1 根门推进版的完整垂直切片。正式入口为 `Assets/Game/Scenes/MainMenu.unity`，随后进入 `Level01.unity`。

已完成并通过成品冒烟验证：

- 主菜单进入关卡、三段固定 Encounter、清场开启 Portal、结果页、重开、死亡和返回菜单。
- 横版移动、跳跃、冲刺、普通攻击、技能和集中输入入口。
- 藤蔓怪、蘑菇、甲虫三种行为；藤蔓触须、毒菌伞、铁甲根三种必掉嫁接。
- 藤鞭攻击、毒雾持续伤害与减速、铁甲根减伤与冲刺挡弹。
- 项目方提供的 168 张运行时 PNG 已恢复并接入；主角攻击、藤蔓怪、蘑菇、孢子/命中特效和遗迹环境可用。
- Windows Release 构建及自动完整流程验证。
- Windows PC 正常 GPU 模式下完成 1280x720、1600x900、1920x1080 三次连续成品回归。

## 本次接手修复

- 从上一轮验证临时副本恢复遗漏的 `Assets/Game/Art` 和全部 `.meta`。
- 恢复场景所依赖的 `PlaceholderVisual`、`CameraFollow2D` 原 GUID，消除 10 个 Missing Script。
- 增加场景 Missing Script 回归测试，美术/动画数量测试恢复通过。
- 补齐左右方向键；`Tab/G` 现在都能打开和关闭嫁接界面。
- 修复死亡延迟期间可进入嫁接而永久暂停死亡结算的软锁。
- 传送门进入阶段锁定移动、攻击和 Gameplay 输入。
- 投射物改为扫掠检测，降低低帧率下穿过受击框的概率。
- 修复毒雾每帧续期导致效果超出技能生命周期，以及到期时漏掉最后一跳伤害的问题；现在 3 秒内固定造成 3 次共 18 点伤害并清除减速。
- 增加地面减速度与最大下落速度配置，并让正式构建清理旧构建缓存。
- 接入 15 个 CC0 音效、Master/Music/SFX Audio Mixer、持久化音量设置及菜单滑杆。
- README 与 Credits 已更新为当前真实资源状态。

## 验证结果

- EditMode：13/13 通过，最新结果文件为 `Build/Validation/PoisonEditModeResults.xml`。
- Windows Release：构建成功，入口为 `Build/Windows/PlantSpirit.exe`。
- 成品自动试玩：正常 GPU 模式连续三次退出码均为 0，覆盖音频设置、Mixer 运行时应用、美术、方向键、嫁接开关、三战区、三嫁接效果、传送门锁定、结果、重开、死亡延迟与返回菜单；对应日志为 `Build/Validation/PC1280x720.log`、`PC1600x900.log`、`PC1920x1080.log`。
- 毒雾修复成品回归：1280x800 正常 GPU 模式退出码 0，强制检查 3 秒毒伤总量为 18 且减速归零；日志为 `Build/Validation/PoisonGpuSmoke.log`。
- 本轮 PC 构建与正常 GPU 成品日志未发现编译错误、运行异常或流程失败。

## 仍未完成

GGJ Demo 规格中的剩余项：

- 背景音乐尚未接入；现有四首候选音乐缺少逐首来源与授权说明。
- 甲虫动画、主角待机/跑/跳/落/冲刺/受击/死亡、Portal、嫁接图标和正式 UI 资产缺失。
- 手柄完整验收按当前范围暂缓；本轮已完成三种 16:9 PC 分辨率的自动连续回归，非 16:9 与人工手感验收仍待后续安排。
- 当前关卡和敌人仍偏 Demo 化：敌人由 Encounter 运行时创建，尚无正式 Prefab、RoomData 或完整数据校验。

V1.1 根门推进版尚未开始的核心内容：

- 可复现 `RunSeed`、手工房间模块、清场后二选一/三选一根门及保底规则。
- 三段轻攻击、进化选择、八种特征、两条融合、六类敌人和腐化古树 Boss。
- 12 至 18 分钟首章流程、净化前后场景切换、存档和完整键鼠/手柄 UI 导航。

## 建议下一里程碑

当前优先级为 Windows PC 成品的可复现 Bug 检查与修复。玩法扩展、手柄适配和正式表现补充均暂缓，避免稳定性阶段扩大改动范围。

若转入 V1.1 垂直切片，先冻结首章 8 个特征与 2 条融合，再实现 `RunSeed + RoomDefinition + RootGate` 的三个灰盒房间闭环；不要直接在现有 `Level01` 大场景里继续硬编码路线。
